pipeline{
    agent any
    stages{
        stage('Build'){
            steps{
                script{
                    if (${Options} == "add"){
                    print("add")
                    }
                    }
        }
    }
}
}